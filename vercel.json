{
  "buildCommand": "npm install --legacy-peer-deps && npm run build --workspace=@casa-segura/web-pro && cp -r apps/web-pro/.next .next && cp -r apps/web-pro/public public 2>/dev/null || true",
  "installCommand": "npm install --legacy-peer-deps"
}
